# MedicTrack React Frontend

This is the React frontend for MedicTrack and it works with the Node.js API.

## Start

```bash
npm install
npm run dev
```

By default, Vite runs on `http://localhost:5173` and proxies `/api` to `http://localhost:4000`.
